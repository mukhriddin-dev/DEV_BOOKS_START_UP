import React from "react";
import {Outlet} from "react-router-dom";
const Layout = () => {
    return (
        <div>
            {/* header */}

            {/* main routing <Outlet/> */}

            {/* footer */}
        </div>
    );
};

export default Layout;
